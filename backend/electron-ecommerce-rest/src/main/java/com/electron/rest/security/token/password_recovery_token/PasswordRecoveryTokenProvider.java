package com.electron.rest.security.token.password_recovery_token;

import com.electron.rest.security.auth_entity.User;
import com.electron.rest.security.token.TokenFactory;

public abstract class PasswordRecoveryTokenProvider implements TokenFactory<User> {
}
