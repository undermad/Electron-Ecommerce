import {BasketPositionsList} from "./BasketPositionsList.tsx";

export const Basket = () => {

    // const basketContext = useContext(BasketContext);

    return (
        <div>
            <BasketPositionsList/>

        </div>
    )
}