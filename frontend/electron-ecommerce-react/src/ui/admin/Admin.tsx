import {useScrollToTop} from "../../custom_hooks/useScrollToTop.ts";

export const Admin = () => {

    useScrollToTop();

    return (
        <div>
            Welcome in the Admin Section!
        </div>
    )
}